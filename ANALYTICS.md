# Google Analytics 적용 완료 ✅

## 📊 적용된 Analytics 이벤트

### 1. **페이지 뷰 추적** (자동)
- `RouteTracker` 컴포넌트를 통해 모든 페이지 전환 자동 추적
- 추적 경로: `/` (랜딩), `/calculator` (계산기)

### 2. **사용자 행동 이벤트**

#### 🐾 반려동물 선택
- **이벤트명**: `select_pet_type`
- **파라미터**: `pet_type` (dog/cat)
- **위치**: `PetTab` 컴포넌트

#### 🧮 계산 완료
- **이벤트명**: `calculation_complete`
- **파라미터**: 
  - `species` (dog/cat)
  - `age` (나이)
  - `weight` (체중)
  - `bcs` (비만도 점수)
  - `is_neutered` (중성화 여부)
  - `dog_size` (강아지 크기: small/medium/large)
- **위치**: `CalculatorPage` - 나이와 체중 입력 시 자동 추적

#### ⚖️ BCS 모드 전환
- **이벤트명**: `toggle_bcs_mode`
- **파라미터**: `mode` (auto/manual)
- **위치**: `CalculatorPage` - BCS 자동/수동 모드 전환 시

#### 🔄 중성화 상태 토글
- **이벤트명**: `toggle_neutered`
- **파라미터**: `is_neutered` (true/false)
- **위치**: `CalculatorPage`

#### 📏 강아지 크기 선택
- **이벤트명**: `select_dog_size`
- **파라미터**: `size` (small/medium/large)
- **위치**: `CalculatorPage`

#### 🔗 외부 링크 클릭
- **이벤트명**: `click_external_link`
- **파라미터**: 
  - `link_type` (diet/joint)
  - `species` (dog/cat)
- **위치**: `ResultCard` - 다이어트/관절 건강 링크 클릭 시

#### 🌐 언어 변경
- **이벤트명**: `change_language`
- **파라미터**: `language` (ko/en)
- **위치**: `LandingPage`

#### 🎯 CTA 버튼 클릭
- **이벤트명**: `click_cta`
- **파라미터**: `button_name` (start_calculator)
- **위치**: `LandingPage` - "시작하기" 버튼

## 📁 파일 구조

```
src/
├── utils/
│   └── analytics.ts          # Analytics 유틸리티 함수
├── components/
│   ├── RouteTracker.tsx       # 페이지 뷰 자동 추적
│   ├── PetTab.tsx            # 반려동물 선택 추적
│   └── ResultCard.tsx        # 외부 링크 클릭 추적
├── pages/
│   ├── LandingPage.tsx       # 언어 변경, CTA 클릭 추적
│   └── CalculatorPage.tsx    # 계산 완료, 각종 입력 추적
└── firebase.ts               # Firebase Analytics 초기화
```

## 🔍 GA4 대시보드에서 확인하는 방법

1. **실시간 데이터 확인**
   - Google Analytics → 실시간 → 이벤트
   - 앱 사용 시 실시간으로 이벤트 발생 확인 가능

2. **주요 지표 분석**
   - **전환율**: `click_cta` 이벤트로 랜딩→계산기 전환율 측정
   - **인기 기능**: `select_pet_type`으로 강아지 vs 고양이 사용 비율
   - **사용 패턴**: `toggle_bcs_mode`로 자동/수동 BCS 선호도
   - **콘텐츠 효과**: `click_external_link`로 다이어트/관절 정보 관심도

3. **맞춤 보고서 생성**
   - 탐색 분석 → 유입경로 탐색
   - 이벤트 순서: `page_view` → `select_pet_type` → `calculation_complete` → `click_external_link`

## 🚀 배포 방법

```bash
npm run deploy
```

배포 후 Firebase Console → Analytics에서 데이터 확인 가능합니다.

## 💡 활용 팁

1. **A/B 테스팅**: 언어별 사용 패턴 비교 (`change_language` 이벤트)
2. **기능 개선**: 가장 많이 사용되는 기능 파악
3. **사용자 여정 분석**: 랜딩 → 계산 → 외부 링크 클릭 흐름 추적
4. **전환 최적화**: CTA 클릭률 모니터링으로 랜딩 페이지 개선

## ✅ 체크리스트

- [x] Firebase Analytics 초기화
- [x] 페이지 뷰 자동 추적
- [x] 주요 사용자 행동 이벤트 추적
- [x] 빌드 테스트 완료
- [ ] 배포 후 실시간 데이터 확인
- [ ] GA4 맞춤 보고서 설정
